# Global Gradle Properties
# These settings apply to all projects in this workspace

# Gradle Build Performance
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# JVM Settings for Gradle
org.gradle.jvmargs=-Xmx2g -XX:+UseG1GC -XX:+UseStringDeduplication

# Kotlin Compiler Settings
kotlin.code.style=official
kotlin.incremental=true
kotlin.incremental.useClasspathSnapshot=true

# Kotlin/JVM Settings
kotlin.jvm.target.validation.mode=warning

# Project Versions (centralized version management)
kotlinVersion=1.9.22
ktorVersion=2.3.7
ktormVersion=3.6.0
logbackVersion=1.4.14

# Build Configuration
buildConfigField.API_BASE_URL="https://api.bybit.com"
buildConfigField.APP_VERSION="0.1.0-SNAPSHOT"

# Copyright Information
project.copyright="Copyright (c) 2025 Vladyslav (github.com/Nanachi357)"

# Development Settings
development=true

# Publishing (if needed later)
group=io.swapter.bybit
version=0.1.0-SNAPSHOT